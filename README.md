# Шаблон проекта Vue 3

Это продвинутый шаблон для нового проекта на [Vue 3](https://vuejs.org/), использующий [Vite](https://vitejs.dev/) для быстрой разработки и сборки, а также [TypeScript](https://www.typescriptlang.org/) для типизации кода. Включает в себя ряд современных инструментов и библиотек для создания высокопроизводительных, надежных и поддерживаемых веб-приложений.

## ✨ Особенности

- **Фреймворк**: [Vue 3](https://vuejs.org/) с использованием `<script setup>`.
- **Сборка**: [Vite](https://vitejs.dev/) — молниеносный инструмент сборки.
- **Типизация**: [TypeScript](https://www.typescriptlang.org/) для статической типизации и повышения надежности кода.
- **Маршрутизация**: [Vue Router](https://router.vuejs.org/) — официальный маршрутизатор для Vue.js.
- **Управление состоянием**: [Pinia](https://pinia.vuejs.org/) — легковесная и интуитивно понятная библиотека.
- **Утилиты**: [VueUse](https://vueuse.org/) — коллекция полезных composable-функций.
- **Стилизация**: [Tailwind CSS](https://tailwindcss.com/) — утилитарный CSS-фреймворк для быстрого создания интерфейсов.
- **UI-компоненты**: [Storybook](https://storybook.js.org/) для разработки, документирования и тестирования UI-компонентов в изоляции.
- **Иконки**: [Unplugin Icons](https://github.com/unplugin/unplugin-icons) для легкого доступа к тысячам иконок.
- **Тестирование**: [Vitest](https://vitest.dev/) — быстрый фреймворк для модульного тестирования, интегрированный с Vite.
- **Статический анализ и форматирование**:
  - [Oxlint](https://oxc-project.github.io/docs/guide/linter.html) — сверхбыстрый линтер для выявления ошибок.
  - [ESLint](https://eslint.org/) с набором плагинов (`security`, `simple-import-sort`, `unicorn`, `unused-imports`) для поддержания качества и стандартов кода.
  - [Prettier](https://prettier.io/) — форматер кода для обеспечения единообразного стиля.
- **Git Hooks**: [Husky](https://typicode.github.io/husky/), [lint-staged](https://github.com/lint-staged/lint-staged) и [commitlint](https://commitlint.js.org/) для автоматического контроля качества кода и коммитов.
- **Анализ проекта**: [Knip](https://knip.dev/) для поиска неиспользуемых файлов, зависимостей и экспортов.
- **Инструменты разработки**: [Vite DevTools](https://github.com/webfansplz/vite-plugin-vue-devtools) для удобной отладки Vue-приложений.

## 🚀 Начало работы

### Требования

Для запуска проекта убедитесь, что у вас установлены:

- [Node.js](https://nodejs.org/) (версия `^20.19.0` или `>=22.12.0`)
- [pnpm](https://pnpm.io/) (рекомендуемый менеджер пакетов)

### Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Loloops123/vue_blank.git
    cd vue_blank
    ```
2.  **Установите зависимости:**
    ```bash
    pnpm install
    ```

### Разработка

Для запуска сервера разработки с инструментами отладки Vue:

```bash
pnpm dev
```

Приложение будет доступно по адресу `http://localhost:5173`. Горячая перезагрузка модулей (HMR) включена по умолчанию.

### Сборка

Для сборки приложения для продакшена:

```bash
pnpm build:ci
```

Эта команда сначала выполнит проверку типов TypeScript, а затем сгенерирует оптимизированные статические файлы в директории `dist/`.

## ⚙️ Доступные скрипты

- `pnpm dev`: Запускает локальный сервер разработки Vite.
- `pnpm storybook`: Запускает сервер разработки Storybook для UI-компонентов.
- `pnpm test:unit`: Запускает модульные тесты с использованием Vitest.
- `pnpm test:storybook`: Запускает тесты для Storybook.
- `pnpm lint:oxlint`: Запускает быстрый линтер Oxlint для поиска ошибок.
- `pnpm lint:eslint`: Запускает ESLint для проверки стиля и качества кода.
- `pnpm lint:types`: Выполняет проверку типов TypeScript во всем проекте.
- `pnpm lint:commit`: Проверяет сообщение последнего коммита на соответствие правилам Conventional Commits.
- `pnpm lint:all`: Последовательно запускает все задачи линтинга (`lint:*`).
- `pnpm build:dev`: Собирает приложение в режиме разработки без проверки типов.
- `pnpm build:ci`: Собирает приложение для продакшена с предварительной проверкой типов.
- `pnpm storybook:build`: Собирает Storybook для развертывания.
- `pnpm prettier:format`: Форматирует весь код проекта с помощью Prettier.
- `pnpm knip`: Запускает Knip для анализа неиспользуемых частей проекта.
- `pnpm prepare`: Настраивает Husky для pre-commit хуков.

## 📁 Структура проекта

```
.
├── .husky/          # Конфигурация Git-хуков
├── .storybook/      # Конфигурация Storybook
├── public/          # Статические активы
└── src/
    ├── assets/      # CSS, шрифты, изображения
    ├── components/  # Переиспользуемые Vue-компоненты
    ├── router/      # Конфигурация Vue Router
    ├── store/       # Модули Pinia
    ├── view/        # Компоненты-страницы
    ├── App.vue      # Корневой компонент приложения
    └── main.ts      # Точка входа в приложение
```

## 🤝 Вклад

Приветствуются любые вклады! Пожалуйста, создавайте `Issues` и `Pull Requests`.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.
