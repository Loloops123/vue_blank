# Шаблон проекта Vue 3

Это шаблон для нового проекта на [Vue 3](https://vuejs.org/), использующий [Vite](https://vitejs.dev/) для быстрой разработки и сборки, а также [TypeScript](https://www.typescriptlang.org/) для типизации кода. Включает в себя ряд современных инструментов и библиотек для создания высокопроизводительных и поддерживаемых веб-приложений.

## ✨ Особенности

- **Vue 3**: Последняя версия прогрессивного JavaScript-фреймворка.
- **Vite**: Молниеносный инструмент сборки, обеспечивающий быструю разработку.
- **TypeScript**: Статически типизированное надмножество JavaScript для повышения надежности кода.
- **Pinia**: Легковесная и интуитивно понятная библиотека для управления состоянием в Vue 3.
- **Vue Router**: Официальный маршрутизатор для Vue.js.
- **Tailwind CSS**: Утилитарный CSS-фреймворк для быстрого создания пользовательских интерфейсов.
- **Storybook**: Инструмент для разработки, документирования и тестирования UI-компонентов в изоляции.
- **Vitest**: Быстрый фреймворк для модульного тестирования, работающий на Vite.
- **ESLint**: Инструмент для статического анализа кода, помогающий поддерживать стандарты кодирования.
- **Prettier**: Форматер кода для обеспечения единообразного стиля.
- **Husky & lint-staged**: Инструменты для автоматического запуска проверок кода перед коммитами.

## 🚀 Начало работы

### Требования

Для запуска проекта убедитесь, что у вас установлены:

- [Node.js](https://nodejs.org/) (рекомендуется LTS-версия)
- [pnpm](https://pnpm.io/) (менеджер пакетов)

### Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Loloops123/vue_blank.git
    cd vue_blank
    ```
2.  **Установите зависимости:**
    ```bash
    pnpm install
    ```

### Разработка

Для запуска сервера разработки:

```bash
pnpm dev
```

Приложение будет доступно по адресу `http://localhost:5173`. Горячая перезагрузка модулей (HMR) включена для мгновенного обновления изменений.

### Сборка

Для сборки приложения для продакшена:

```bash
pnpm build:ci
```

Эта команда сгенерирует оптимизированные статические файлы в директории `dist/`.

## ⚙️ Доступные скрипты

В файле `package.json` определены следующие скрипты:

- `pnpm dev`: Запускает локальный сервер разработки.
- `pnpm storybook`: Запускает сервер разработки [Storybook](https://storybook.js.org/) для изоляции компонентов.
- `pnpm test:unit`: Запускает модульные тесты с использованием [Vitest](https://vitest.dev/).
- `pnpm test:storybook`: Запускает тесты Storybook.
- `pnpm lint:all`: Выполняет проверку и форматирование кода с помощью ESLint и Prettier.
- `pnpm build:dev`: Собирает приложение в режиме разработки.
- `pnpm build:ci`: Собирает приложение для продакшена (используется для CI/CD).
- `pnpm storybook:build`: Собирает Storybook для развертывания.
- `pnpm prettier:format`: Форматирует код с помощью Prettier.
- `pnpm knip`: Запускает [Knip](https://knip.dev/) для поиска неиспользуемых файлов, зависимостей и экспортов.
- `pnpm prepare`: Настраивает [Husky](https://typicode.github.io/husky/) для Git-хуков (например, pre-commit).

## 🤝 Вклад

Приветствуются любые вклады! Пожалуйста, ознакомьтесь с [CONTRIBUTING.md](CONTRIBUTING.md) (если есть) для получения дополнительной информации.

## 📄 Лицензия

Этот проект распространяется под лицензией [MIT License](LICENSE) (если применимо).
